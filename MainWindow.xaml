<Window x:Class="CybersecurityChatbot.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Cybersecurity Awareness Chatbot" 
        Height="750" Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="#1a1a2e"
        ResizeMode="CanResize"
        MinHeight="600" MinWidth="800">

    <Window.Resources>
        <SolidColorBrush x:Key="BackgroundDark" Color="#1a1a2e"/>
        <SolidColorBrush x:Key="BackgroundMedium" Color="#16213e"/>
        <SolidColorBrush x:Key="AccentColor" Color="#0f3460"/>
        <SolidColorBrush x:Key="HighlightColor" Color="#e94560"/>

        <Style x:Key="UserBubble" TargetType="Border">
            <Setter Property="Background" Value="#e94560"/>
            <Setter Property="CornerRadius" Value="15,15,5,15"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="Margin" Value="80,5,10,5"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="5" ShadowDepth="2" Opacity="0.3"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="BotBubble" TargetType="Border">
            <Setter Property="Background" Value="#0f3460"/>
            <Setter Property="CornerRadius" Value="15,15,15,5"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="Margin" Value="10,5,80,5"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="5" ShadowDepth="2" Opacity="0.3"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="UserText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>

        <Style x:Key="BotText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#4ecca3"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>

        <Style x:Key="TimestampStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#888888"/>
            <Setter Property="FontSize" Value="9"/>
            <Setter Property="Margin" Value="0,5,0,0"/>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#e94560"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="MinWidth" Value="60"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#c73e54"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="#0f3460"/>
            <Setter Property="Foreground" Value="#4ecca3"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="MinWidth" Value="60"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1a4a7a"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ChatTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#0f3460"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="CaretBrush" Value="#4ecca3"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title Bar -->
        <Border Grid.Row="0" Background="#0f3460" Padding="12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="[SHIELD]" FontSize="20" FontWeight="Bold" Foreground="#4ecca3" Margin="0,0,10,0"/>
                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center">
                    <TextBlock Text="CYBERSECURITY" FontSize="22" FontWeight="Bold" Foreground="#e94560"/>
                    <TextBlock Text=" AWARENESS " FontSize="22" FontWeight="Bold" Foreground="White"/>
                    <TextBlock Text="CHATBOT" FontSize="22" FontWeight="Bold" Foreground="#4ecca3"/>
                </StackPanel>
                <TextBlock Grid.Column="2" Text="v2.0" FontSize="12" Foreground="#888888" VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <!-- Status Bar -->
        <Border Grid.Row="1" Background="#16213e" Padding="8,5" Margin="10,5,10,0" CornerRadius="8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <Ellipse Width="10" Height="10" Fill="#4ecca3" Margin="0,0,5,0"/>
                    <TextBlock Text="Connected" FontSize="11" Foreground="#4ecca3"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center">
                    <TextBlock Text="Current Topic: " FontSize="11" Foreground="#888888"/>
                    <TextBlock x:Name="CurrentTopicLabel" Text="None" FontSize="11" Foreground="#e94560" FontWeight="Bold"/>
                </StackPanel>
                <TextBlock x:Name="MessageCountLabel" Grid.Column="2" Text="Messages: 0" FontSize="11" Foreground="#888888"/>
            </Grid>
        </Border>

        <!-- ASCII Art -->
        <Border Grid.Row="2" Background="#16213e" Margin="10,10,10,5" CornerRadius="10" Padding="12">
            <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" MaxHeight="150">
                <TextBlock x:Name="AsciiArtDisplay" FontFamily="Consolas" FontSize="9" Foreground="#4ecca3" TextAlignment="Center"/>
            </ScrollViewer>
        </Border>

        <!-- Chat Area -->
        <Border Grid.Row="3" Background="#1a1a2e" Margin="10,5,10,5" CornerRadius="10">
            <ScrollViewer x:Name="ChatScrollViewer" VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="ChatMessagesPanel" Margin="10"/>
            </ScrollViewer>
        </Border>

        <!-- Input Area -->
        <Border Grid.Row="4" Background="#16213e" Margin="10,5,10,15" CornerRadius="15" Padding="12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Background="#0f3460" CornerRadius="15" Padding="10,5">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock x:Name="SentimentIcon" Text=":-|" FontSize="14" Margin="0,0,5,0"/>
                            <TextBlock x:Name="SentimentLabel" Text="Mood: Neutral" Foreground="#4ecca3" FontSize="11" FontWeight="Bold"/>
                        </StackPanel>
                    </Border>

                    <TextBlock Grid.Column="1" Text=""/>

                    <Border Grid.Column="2" Background="#0f3460" CornerRadius="15" Padding="10,5" Margin="10,0,0,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="[MEMORY]" FontSize="10" Margin="0,0,5,0"/>
                            <TextBlock x:Name="MemoryStatusLabel" Text="Active" Foreground="#4ecca3" FontSize="11"/>
                        </StackPanel>
                    </Border>

                    <Button x:Name="VoiceButton" Grid.Column="3" Content="Voice" Style="{StaticResource SecondaryButton}" Width="65" Margin="10,0,0,0" Click="VoiceButton_Click"/>
                    <Button x:Name="ClearButton" Grid.Column="4" Content="Clear" Style="{StaticResource SecondaryButton}" Width="65" Margin="5,0,0,0" Click="ClearButton_Click"/>
                    <Button x:Name="SessionButton" Grid.Column="5" Content="Info" Style="{StaticResource SecondaryButton}" Width="55" Margin="5,0,0,0" Click="SessionButton_Click"/>
                </Grid>

                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Background="#0f3460" CornerRadius="25" Margin="0,0,10,0">
                        <Grid>
                            <TextBox x:Name="UserInput" Style="{StaticResource ChatTextBox}" KeyDown="UserInput_KeyDown" MinHeight="40" MaxHeight="100" VerticalContentAlignment="Center"/>
                            <TextBlock Text="Type your cybersecurity question here..." Foreground="#666666" FontSize="13" IsHitTestVisible="False" Margin="12,0,0,0" VerticalAlignment="Center">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Visibility" Value="Visible"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Text, ElementName=UserInput}" Value="">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Text.Length, ElementName=UserInput}" Value="0">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Text.Length, ElementName=UserInput}" Value="1">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Grid>
                    </Border>

                    <Button x:Name="SendButton" Grid.Column="1" Content="Send" Style="{StaticResource PrimaryButton}" Width="80" Height="40" Click="SendButton_Click"/>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Window>